# ---------------------------------------------------------------------------
# Shotbuddy environment example
# Copy this file to /etc/shotbuddy.env (or source it in your shell)
# ---------------------------------------------------------------------------

# Absolute root directory where Shotbuddy stores/creates projects when a user
# supplies a relative path.
SHOTBUDDY_BASE_DIR=/srv/shotbuddy/projects

# Scratch directory for uploads and generated thumbnails.
SHOTBUDDY_UPLOAD_FOLDER=/srv/shotbuddy/uploads

# rclone remote (configured via "rclone config") used as canonical asset store.
# Example targets:
#   do_spaces:shotbuddy-assets   (DigitalOcean Spaces)
#   gdrive:Shotbuddy             (Google Drive)
RCLONE_REMOTE=do_spaces:shotbuddy-assets

# Token required for API access. Leave empty to disable authentication.
SHOTBUDDY_TOKEN=ChangeMe123

# Optional: bind address and port for Flask (systemd service overrides).
SHOTBUDDY_HOST=0.0.0.0
SHOTBUDDY_PORT=5001

# Uncomment for verbose logging during troubleshooting
# SHOTBUDDY_DEBUG=1

# Python environment tweaks (only if you know what you’re doing)
# PYTHONUNBUFFERED=1 